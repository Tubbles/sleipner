# Prompt for agent

* i notice that you sometimes struggle with some lint checks, lets make a habit to write those checks dowm in CLAUDE.md,
  along with a note to keep adding to that list. stuff like implicit boolean conversions and confusable parameters
