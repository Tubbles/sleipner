name: Android

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Android toolchain image
        run: docker build -f Dockerfile.android -t sleipner-toolchain-android .

      - name: Build APK
        run: IMAGE=sleipner-toolchain-android CONTAINER_CMD=docker ./ci.sh apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: sleipner-android-arm64
          path: android/app/build/outputs/apk/release/*.apk
